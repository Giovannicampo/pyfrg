# Stato del Progetto: PyForgeryMaker

## Data: 21 Dicembre 2025

### ✅ Cosa abbiamo fatto oggi:
1. **Definizione Progetto:** Creato il concept di un tool per "Image Forgery" (Copy-Move, Splicing, Inpainting).
2. **Struttura Directory:** Creata la cartella radice `PyForgeryMaker/` con le sottocartelle `core/` e `gui/`.
3. **Ambiente:** Creato `requirements.txt` con `customtkinter`, `opencv`, `pillow` e `numpy`.
4. **Entry Point:** Creato `main.py` per l'avvio.
5. **GUI Base:** Implementato `gui/app.py` con una finestra moderna, sidebar e logica di caricamento file (mock).

### 🚀 Prossimi Passi (per domani):
1. **CanvasWidget (`gui/canvas_widget.py`):** Creare il widget per visualizzare l'immagine e permettere la selezione rettangolare con il mouse.
2. **ImageProcessor (`core/image_processor.py`):** Implementare la prima funzione di Copy-Move usando OpenCV e Numpy.
3. **Integrazione:** Collegare il Canvas alla logica di processamento per vedere i primi risultati di manipolazione.

---
*Ci vediamo domani per iniziare a lavorare sulla selezione interattiva!*
